# Nora Videoplatform

## Deploy
Wenn der User "push" sagt oder bittet zu deployen, fuehre automatisch `git push` aus. Das Deployment laeuft dann automatisch ueber GitHub Actions (.github/workflows/deploy.yml).
